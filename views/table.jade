extends layout

block content
    .row
        each title in titles
            +titleRow(title)
    each city in cities
        .row
            each info in city
                +contentRow(info)


    mixin titleRow(title)
        .row-title
            h3= title
    mixin contentRow(content)
        .row-content
            h5= content